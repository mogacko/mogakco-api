# 사용자 탈퇴 시 이벤트 도메인 고려사항

> 현재 구현되지 않은 USER 탈퇴 및 장기 개인정보 파기 작업만 관리한다.

## 소프트 딜리트

- 종료되지 않은 이벤트의 참가 행은 삭제하고 `current_count`를 함께 감소시킨다.
- 사용자가 호스트인 미종료 `PENDING` 및 `APPROVED` 이벤트는 `CANCEL` 처리한다.
- API에서 탈퇴 사용자의 정보가 노출되는 경우 `deleted_at`을 확인하여 UUID와 닉네임을 댓글처럼 마스킹한다.

## 장기 물리 삭제

- 보유기간이 끝나면 종료 이벤트 참가 행의 `user_id`를 `NULL`로 익명화한다.
- `event_participants.user_id`의 nullable 변경은 장기 개인정보 파기 기능과 함께 구현한다.

## 추가 검토사항

- 호스트 탈퇴로 행사가 취소될 때 참가자 알림과 유료 행사 환불을 함께 처리한다.
