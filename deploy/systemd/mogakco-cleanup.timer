[Unit]
Description=모각코 고아 이미지 정리를 하루 한 번 실행

[Timer]
OnCalendar=daily

# 미니PC는 재부팅·절전·정전으로 꺼져 있는 시간이 있다. 그때 놓친 실행을
# 부팅 직후 따라잡는다. 이게 없으면 그날치는 그냥 건너뛴다.
Persistent=true

# 다른 일과 정각에 겹치지 않게 흩는다.
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
