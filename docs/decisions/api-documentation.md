# API 문서화 결정

Swagger 제목은 라우트별 한국어 `summary`로 지정한다. 복잡한 흐름만 docstring으로 설명한다. 전역 OpenAPI 설정은 제목·태그 설명만 관리하며, 공통 래퍼나 후처리는 사용하지 않는다.

`GET /health`는 `{"ok": true}`만 돌려주는 인프라용 엔드포인트다. 로드밸런서와 컨테이너 헬스 체크가 부른다. 인증이 없고 어느 기능 모듈에도 속하지 않아 태그 없이 `app/main.py`에 둔다.
