# 계획 문서 관리 결정

기능별 계획 문서는 `docs/plan/<feature>.md`로 하나만 관리한다. 구현·검증이 끝나면 파일명은 유지한 채 `docs/complete/<feature>.md`로 이동한다.

폴더가 상태를 표현하므로 파일명에는 `plan`, `complete` 같은 상태 접미사나 기능별 하위 폴더를 사용하지 않는다. 계획 문서에는 기능 계약, 구현 체크리스트, 검증 결과를 함께 기록한다.
